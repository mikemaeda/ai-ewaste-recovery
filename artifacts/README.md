# Artifacts

This directory stores generated models and reports.

Model weights are ignored because they can be large:

```text
artifacts/models/
```

Public summary reports and poster-ready figures may be committed when they are small enough for GitHub:

```text
artifacts/reports/
```
